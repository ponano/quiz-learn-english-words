# Викторина: Угадай пару

Интерактивная веб-викторина для изучения английских слов и их русских переводов. Пользователь соединяет английские слова с соответствующими переводами, отслеживая свой прогресс и точность. Проект демонстрирует навыки работы с HTML, CSS и JavaScript, включая динамическое обновление DOM, управление состоянием и адаптивный интерфейс.

[Попробовать викторину](https://ponano.github.io/quiz-learn-english-words/)

## Функционал

- **10 пар слов на листе**: Каждый лист содержит 10 пар английских слов и их русских переводов.
- **Уникальные переводы**: Русские переводы на каждом листе уникальны, чтобы избежать путаницы.
- **Форматирование слов**: Русские переводы отображаются с маленькой буквы, английские слова — без артикля "To" (например, "be" вместо "To be").
- **Интерактивный выбор**: Пользователь кликает на английское слово и его перевод. Поддерживается отмена выбора (повторный клик снимает выделение).
- **Подсветка**:
  - Синий цвет — выбранное слово.
  - Зелёный — правильная пара.
  - Красный — ошибка (исчезает через 1 секунду).
- **Прогресс и статистика**:
  - Прогресс-бар показывает процент пройденных слов (из 116).
  - Текст прогресса: "Лист X из Y | Пройдено Z/116 слов".
  - Статистика: "Правильных ответов: A из B (C%)".
- **Переход между листами**: После завершения листа (10 пар) автоматически загружается следующий.
- **Порог прохождения**: Викторина считается пройденной при точности ≥95% (по всем попыткам).
- **Шеринг результата**:
  - Кнопка "Поделиться результатом" появляется после завершения всех листов.
  - Поддерживает `navigator.share` (на устройствах, где доступно) или копирование в буфер обмена.
  - Пример текста: "Я прошел викторину 'Угадай пару' с результатом 95.00% (110 правильных из 116 попыток)! 🎉".
- **Перезапуск**:
  - Кнопка "Начать с начала" показывает предупреждение (`confirm`) перед сбросом прогресса.
  - Сбрасывает все: слова, статистику, прогресс-бар, состояние.
- **Адаптивность**: Интерфейс удобен на десктопах и мобильных устройствах.

## Скриншоты

*Основной экран с выбором слов и прогресс-баром*

![Викторина: Основной интерфейс](https://github.com/user-attachments/assets/d01f23f1-60b1-4bd5-aac2-b5c7d879d49f)

## Технологии

- **HTML**: Структура страницы, включая контейнеры для слов, статистики и кнопок.
- **CSS**: Стилизация (Flexbox для колонок, адаптивный дизайн, анимации подсветки).
- **JavaScript**: Логика викторины, управление состоянием, обработка событий, динамическое обновление DOM.
- **GitHub Pages**: Деплой проекта для публичного доступа.

## Логика работы

### 1. Инициализация
- При загрузке страницы (`DOMContentLoaded`):
  - Создаётся массив `batches` с листами по 10 пар слов (`createBatches`).
  - Отображается первый лист (`displayBatch`).
  - Привязывается обработчик события для кнопки "Начать с начала" (`restartQuiz`).
- Переменные состояния:
  - `currentBatch`: Номер текущего листа (начинается с 0).
  - `selectedEn`, `selectedRu`: Выбранные английское и русское слова.
  - `correctAnswers`, `totalAttempts`: Счётчики правильных и общих попыток.
  - `matchedPairs`: Массив угаданных пар.
  - `batchAttempts`: Счётчик попыток в текущем листе.
  - `batches`: Массив листов, каждый содержит до 10 пар слов.
  - `totalWords`: Всего слов (116).

### 2. Формирование листов (`createBatches`)
- Список пар (`wordPairs`) перемешивается (`shuffle`).
- Пары делятся на листы по 10 слов.
- Для каждого листа фильтруются только уникальные русские переводы (чтобы исключить дубликаты, например, "включать").
- Если лист содержит хотя бы одну пару, он добавляется в `batches`.

### 3. Отображение листа (`displayBatch`)
- Очищаются контейнеры `#english-words` и `#russian-words`.
- Для текущего листа (`batches[currentBatch]`):
  - Английские слова отображаются в левой колонке (без "To").
  - Русские переводы — в правой колонке, в случайном порядке.
- Каждое слово — это `<div>` с классом `word`, к которому привязывается обработчик клика (`selectWord`).
- Обновляются статистика (`updateStats`) и прогресс (`updateProgress`).

### 4. Выбор слов (`selectWord`)
- Пользователь кликает на слово в одной из колонок:
  - Если слово уже выбрано, повторный клик снимает выделение (отмена выбора).
  - Выбранное слово подсвечивается синим (класс `selected`).
  - Нельзя выбрать уже угаданные слова (класс `correct`).
- Если выбраны оба слова (английское и русское), вызывается `checkPair`.

### 5. Проверка пары (`checkPair`)
- Увеличивается `totalAttempts` и `batchAttempts`.
- Если `dataset.word` английского и русского слова совпадают:
  - Пара подсвечивается зелёным (класс `correct`), добавляется в `matchedPairs`, увеличивается `correctAnswers`.
- Иначе:
  - Пара подсвечивается красным (класс `incorrect`) на 1 секунду.
- Сбрасываются выбранные слова (`selectedEn`, `selectedRu`).
- Обновляются статистика и прогресс.
- Если `batchAttempts` достигает количества пар в листе, переключается на следующий лист или завершает викторину (`checkQuizCompletion`).

### 6. Прогресс и статистика
- **Прогресс-бар** (`#progress-bar`): Ширина = `(totalAttempts / totalWords) * 100%`.
- **Текст прогресса** (`#progress`): "Лист X из Y | Пройдено Z/116 слов".
- **Статистика** (`#stats`): "Правильных ответов: A из B (C%)", где C — процент точности.

### 7. Завершение викторины (`checkQuizCompletion`)
- Вычисляется точность: `(correctAnswers / totalAttempts) * 100`.
- Если точность ≥95%:
  - Показывается: "Поздравляем! Вы прошли викторину с X% правильных ответов!".
  - Колонки слов блокируются (класс `disabled`).
- Иначе:
  - Показывается: "Викторина завершена. Ваш результат: X%. Нужно 95% или выше для прохождения.".
- Показывается кнопка "Поделиться результатом".

### 8. Шеринг результата (`shareResult`)
- Формируется текст результата (включает точность и попытки).
- Если поддерживается `navigator.share`, открывается системное меню шеринга.
- Иначе текст копируется в буфер обмена (`navigator.clipboard`).
- Показывается уведомление: "Результат скопирован в буфер обмена!" (2 секунды).

### 9. Перезапуск (`restartQuiz`)
- При клике на "Начать с начала" показывается `confirm`: "Вы уверены, что хотите начать заново? Прогресс будет потерян.".
- Если подтверждено:
  - Сбрасываются переменные: `currentBatch`, `correctAnswers`, `totalAttempts`, и др.
  - Очищаются DOM-элементы: `#english-words`, `#russian-words`, `#stats`, `#result`, `#share-button`, `#share-message`.
  - Колонки слов разблокируются (удаляется класс `disabled`).
  - Создаются новые листы (`createBatches`) и отображается первый (`displayBatch`).
- Если отмена, ничего не происходит.

### 10. Отладка
- Логирование в консоль:
  - Загрузка DOM, привязка обработчиков, создание листов, отображение, перезапуск.
  - Ошибки: отсутствие DOM-элементов, пустые листы, проблемы с шерингом.
- Резервный `confirm` (с `alert`) для случаев, когда основной не работает.

## Структура кода

- **`index.html`**:
  - HTML: Контейнеры для слов, прогресс-бара, статистики, кнопок.
  - CSS: Flexbox для колонок, стили для подсветки, адаптивность.
  - JavaScript: Логика викторины, управление состоянием, события.

## Как запустить

1. **Онлайн**:
   - Откройте [викторину](https://ponano.github.io/quiz-learn-english-words/) в браузере.
2. **Локально**:
   - Склонируйте репозиторий:
     ```bash
     git clone https://github.com/ponano/quiz-learn-english-words.git
     cd quiz-learn-english-words
     ```
   - Запустите локальный сервер:
     ```bash
     python -m http.server
     ```
   - Откройте `http://localhost:8000/` в браузере.

**Примечание**: Загрузка через `file://` может блокировать функции (`confirm`, `navigator.share`). Используйте локальный сервер.

## Установка и настройка

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/ponano/quiz-learn-english-words.git
   ```
2. Разместите `index.html` в корне репозитория.
3. Залейте изменения:
   ```bash
   git add index.html
   git commit -m "Добавлен файл викторины"
   git push origin main
   ```
4. Настройте GitHub Pages:
   - В репозитории → **Settings** → **Pages**.
   - Выберите ветку `main` и папку `/ (root)`.
   - Дождитесь деплоя (10–15 минут).
   - Доступ: `https://ponano.github.io/quiz-learn-english-words/`.

## Что я узнал

- Динамическое обновление DOM с помощью JavaScript.
- Управление состоянием приложения (переменные, массивы).
- Обработка событий (клики, шеринг, перезапуск).
- Адаптивный дизайн с Flexbox.
- Работа с Git и GitHub Pages для деплоя.
- Отладка с использованием консоли браузера.

## Возможные улучшения

- Добавить кастомное модальное окно вместо `confirm`.
- Добавить анимации переходов между листами.
- Сохранять прогресс в `localStorage`.
- Добавить уровни сложности (например, 5/10/15 слов на лист).

## Автор

[Ponano] ([GitHub](https://github.com/ponano))

---

**Лицензия**: MIT
